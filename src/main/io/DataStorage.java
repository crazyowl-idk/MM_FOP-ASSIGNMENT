public class DataStorage implements Serializable{

        // Player stats
        int level;
        int maxLife;
        int life;
        int maxMana;
        int strength;
        int dexterity;
        int exp;
        int nextLevelExp;
        int  coin;
        int mana;

        //Player inventory
        ArrayList<String> itemNames = new ArrayList<>();
        ArrrayList<Integer> itemAmounts = new ArrayList <> (): 
        int currentWeaponsSlot;
        int currentShieldSlot;

        /* add in player side 
        public int getCurrentWeaponSlot(){
            int currentWeaponSlot = 0;
            for (int i =0; i< inventory.szie(); i++){
                if (inventory.get(i) == currentWeapon){
                    currentWeaponSlot = i; 
                     }
                    }
                    return currentWeaponSlot();

                     public int getCurrentShieldSlot(){
                        int currentShiledSlot = 0;
                        for (int i =0; i< inventory.szie(); i++){
                          if (inventory.get(i) == currentWeapon){
                            currentWeaponSlot = i; 
                     }
                    }
                    return currentWeaponSlot();
            } */
            } */

    }
